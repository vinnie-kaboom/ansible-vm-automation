---

# VM environment (e.g., "production", "staging", "development")
config_vm_environment: "unknown"

# VM hostname (defaults to config_vm_name if not set)
config_vm_host_name: "{{ config_vm_name | default('unknown') }}"
