---
# vmware.vmware collection migration note:
# Guest OS customization is now handled by vmware.vmware.vm_apply_customization
# in create_vm.yml, using unix_prep/windows_sysprep parameters directly.
# The old linux_customization/windows_customization dicts are no longer needed.
