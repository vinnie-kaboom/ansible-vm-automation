---
# ===================================================================================
# Network: GADHQVMESDS02‑OrgEngr — subnet and host‑level configuration
# ===================================================================================

config_vm_network: "GAXXXXXXXXX-XXXXX-xx.x.xxx.xx-xx"
config_vm_domain: "xxxx.xxxxx.local"
config_vm_network_use_dhcp: false
config_vm_network_dns_servers: "xx.xx.xxx.xxx"
config_vm_network_gateway: "xx.x.xxx.x"
config_vm_network_netmask: "xxx.xxx.xxx.0"

# Per‑host derived values
config_vm_name: "{{ inventory_hostname }}"
config_vm_network_start_ip: "{{ vm_host_ip }}"
config_vm_network_end_ip: "{{ vm_host_ip }}"
config_vm_network_domain: "{{ config_vm_domain }}"

# Default VM sizing
config_vm_memory_mb: 4096
config_vm_cpu_count: 2
config_vm_disk_size_gb: 60
config_vm_disk_type: "thin"